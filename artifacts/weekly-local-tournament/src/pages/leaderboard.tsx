import { useMemo, useState } from 'react';
import { ArrowDown, ArrowUp, Minus, Search } from 'lucide-react';
import { useListGames, useListLeaderboards } from '@workspace/api-client-react';
import { Avatar, EmptyState, ErrorState, GameMark, SectionHeading, Skeleton } from '@/components/shared';

export default function Leaderboard() {
  const [game, setGame] = useState('');
  const [search, setSearch] = useState('');
  const gamesQuery = useListGames();
  const query = useListLeaderboards(game ? { game } : undefined);
  const entries = useMemo(() => (query.data ?? []).filter((entry) => !search || `${entry.player} ${entry.tag}`.toLowerCase().includes(search.toLowerCase())), [query.data, search]);
  const podium = entries.slice(0, 3);
  return (
    <div className="mx-auto max-w-[1180px] px-5 py-10 md:px-10 md:py-14">
      <div className="animate-rise flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="font-mono-ui text-[10px] font-bold uppercase tracking-[.2em] text-primary">The local power table</p><h1 className="mt-3 font-display text-5xl font-bold tracking-[-.065em] md:text-7xl">Ranked by<br /><span className="text-primary">showing up.</span></h1></div><p className="max-w-sm text-sm leading-relaxed text-muted-foreground">Points are earned in the arena, not in the comments. See who is setting the pace this week.</p></div>
      {query.isLoading ? <div className="mt-12 grid gap-5 md:grid-cols-3">{[1, 2, 3].map((i) => <Skeleton key={i} className="h-52" />)}</div> : query.isError ? <div className="mt-12"><ErrorState onRetry={() => query.refetch()} /></div> : <><div className="mt-12 grid items-end gap-4 md:grid-cols-3">{podium.map((entry, index) => <div key={entry.tag} className={`relative rounded-2xl border p-6 text-center ${index === 0 ? 'order-first border-primary bg-secondary text-secondary-foreground md:order-2 md:pb-9' : index === 1 ? 'md:order-1 bg-card' : 'md:order-3 bg-card'}`} data-testid={`card-podium-${entry.rank}`}><span className={`absolute left-1/2 top-4 -translate-x-1/2 font-mono-ui text-[10px] font-bold tracking-widest ${index === 0 ? 'text-accent' : 'text-primary'}`}>0{entry.rank}</span><div className="mt-8 flex justify-center"><Avatar name={entry.player} src={entry.avatar} size="lg" /></div><h3 className="mt-4 font-display text-xl font-bold">{entry.player}</h3><p className={`mt-1 font-mono-ui text-[10px] ${index === 0 ? 'text-secondary-foreground/55' : 'text-muted-foreground'}`}>{entry.tag}</p><p className={`mt-5 font-display text-3xl font-bold ${index === 0 ? 'text-accent' : 'text-primary'}`}>{entry.points.toLocaleString()}</p><p className={`text-[10px] uppercase tracking-widest ${index === 0 ? 'text-secondary-foreground/45' : 'text-muted-foreground'}`}>points</p></div>)}</div><div className="mt-16"><SectionHeading eyebrow="Full standings" title="Leaderboard" action={<div className="flex items-center gap-2"><div className="relative hidden sm:block"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Find a player" className="h-10 w-44 rounded-xl border border-input bg-card pl-9 pr-3 text-xs outline-none focus:border-primary" data-testid="input-search-leaderboard" /></div><select value={game} onChange={(event) => setGame(event.target.value)} className="h-10 rounded-xl border border-input bg-card px-3 text-xs font-bold outline-none focus:border-primary" data-testid="select-leaderboard-game"><option value="">All games</option>{(gamesQuery.data ?? []).map((item) => <option value={item.slug} key={item.id}>{item.shortName || item.name}</option>)}</select></div>} />{entries.length ? <div className="overflow-hidden rounded-2xl border border-border bg-card"><div className="hidden grid-cols-[72px_1fr_150px_90px_90px_110px] items-center gap-4 border-b border-border bg-muted/60 px-5 py-3 font-mono-ui text-[9px] font-bold uppercase tracking-[.14em] text-muted-foreground md:grid"><span>Rank</span><span>Player</span><span>Game</span><span>Matches</span><span>Wins</span><span className="text-right">Points</span></div>{entries.map((entry) => <div key={`${entry.rank}-${entry.tag}`} className="grid grid-cols-[42px_1fr_auto] items-center gap-3 border-b border-border px-4 py-4 last:border-0 md:grid-cols-[72px_1fr_150px_90px_90px_110px] md:gap-4 md:px-5"><div className="font-display text-lg font-bold text-muted-foreground">{String(entry.rank).padStart(2, '0')}</div><div className="flex min-w-0 items-center gap-3"><Avatar name={entry.player} src={entry.avatar} size="sm" /><div className="min-w-0"><p className="truncate text-sm font-bold">{entry.player}</p><p className="font-mono-ui text-[10px] text-muted-foreground">{entry.tag}</p></div></div><div className="hidden items-center gap-2 text-xs font-semibold md:flex"><GameMark name={entry.game} size="sm" />{entry.game}</div><div className="hidden text-sm text-muted-foreground md:block">{entry.matches}</div><div className="hidden text-sm text-muted-foreground md:block">{entry.wins}</div><div className="text-right"><p className="font-display font-bold">{entry.points.toLocaleString()}</p><Trend trend={entry.trend} /></div></div>)}</div> : <EmptyState title="No players found" copy="Try another tag or switch to a different game." />}</div></>}
    </div>
  );
}

function Trend({ trend }: { trend: string }) {
  if (trend === 'UP') return <span className="mt-1 inline-flex items-center gap-1 font-mono-ui text-[9px] text-[#5d7900]"><ArrowUp size={11} /> rising</span>;
  if (trend === 'DOWN') return <span className="mt-1 inline-flex items-center gap-1 font-mono-ui text-[9px] text-primary"><ArrowDown size={11} /> slipped</span>;
  return <span className="mt-1 inline-flex items-center gap-1 font-mono-ui text-[9px] text-muted-foreground"><Minus size={11} /> steady</span>;
}